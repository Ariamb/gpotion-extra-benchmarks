#include <stdio.h>
#include <stdlib.h>

int main() {
    unsigned char* myArray = (unsigned char*)malloc(2048 * 2048 * 4);

    if (myArray == NULL) {
        printf("Memory allocation failed\n");
        return 1;
    }
    printf("amogus\n");

    printf("cock sex\n");

    // You can now use 'myArray' to store and manipulate data.
    
    // Don't forget to free the allocated memory when you're done with it.
    free(myArray);

    return 0;
}
